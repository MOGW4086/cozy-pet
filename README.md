# cozy-pet

Flask + SQLite で動くたまごっち風ペット育成Webアプリ。

## セットアップ

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python init_db.py
python run.py
```

ブラウザで http://localhost:5002 を開く。
